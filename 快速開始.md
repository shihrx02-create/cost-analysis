# 成本分析系統 - 快速開始指南

## 🎯 三種部署方式

### 方案 1️⃣：在公司伺服器運行（推薦 - 永久運行）

適用於：想要系統 24/7 運行，無論誰開機都能訪問

**步驟：**
1. 將整個 `成本分析` 資料夾複製到公司伺服器
2. 在伺服器上執行：
   ```bash
   ./setup_and_run.sh  # macOS/Linux
   # 或
   setup_and_run.bat   # Windows
   ```
3. 員工可訪問：`http://伺服器IP:8501`

**保持持續運行：**
```bash
# 使用 screen (簡單)
screen -S cost-analysis
./setup_and_run.sh
# 按 Ctrl+A 再按 D 離開

# 使用 tmux (推薦)
tmux new-session -d -s cost-analysis
tmux send-keys -t cost-analysis "./setup_and_run.sh" Enter
```

---

### 方案 2️⃣：在你的 Mac 上持續運行（簡單）

適用於：你的 Mac 始終開著，其他員工網路存取

**步驟：**
1. 在你的 Mac 上執行：
   ```bash
   cd /Users/grace/Desktop/成本分析
   ./setup_and_run.sh
   ```
2. 員工訪問：`http://你的Mac.local:8501` 或 `http://你的IP:8501`

---

### 方案 3️⃣：員工各自在本機運行

適用於：簡單快速，員工自主使用

**步驟：**
1. 分發 `成本分析` 資料夾給員工
2. 員工雙擊執行：
   - **macOS**: `setup_and_run.sh`
   - **Windows**: `setup_and_run.bat`
3. 瀏覽器自動開啟 `http://localhost:8501`

---

## 🚀 立即開始

**第一次啟動（自動設置）：**
```bash
cd /Users/grace/Desktop/成本分析
./setup_and_run.sh
```

**之後啟動（直接執行）：**
```bash
cd /Users/grace/Desktop/成本分析
.venv/bin/streamlit run app.py
```

---

## 📋 檔案說明

| 檔案 | 說明 |
|------|------|
| `app.py` | 主應用程式 |
| `requirements.txt` | Python 依賴清單 |
| `setup_and_run.sh` | macOS/Linux 自動部署腳本 |
| `setup_and_run.bat` | Windows 自動部署腳本 |
| `部署說明.md` | 詳細的伺服器部署指南 |
| `3-041004-032PN-0.html` | HTML 格式範本 |

---

## ⚙️ 常用命令

```bash
# 啟動應用
./setup_and_run.sh

# 在指定端口啟動
streamlit run app.py --server.port 9000

# 允許其他電腦訪問
streamlit run app.py --server.address 0.0.0.0

# 完整配置
streamlit run app.py \
  --server.address 0.0.0.0 \
  --server.port 8501 \
  --logger.level=info
```

---

## 🔧 故障排除

**問題：無法找到 Python**
- 答：安裝 Python 3.8+ (https://www.python.org)

**問題：模組缺失錯誤**
- 答：執行 `pip install -r requirements.txt`

**問題：端口已被佔用**
- 答：改用其他端口 `streamlit run app.py --server.port 9000`

**問題：無法訪問？**
- 答：確認防火牆設定或改用 localhost 訪問

---

## 📞 技術支援

如有問題，請檢查：
1. Python 版本是否 3.8+
2. 網路連線（自動翻譯需要網際網路）
3. 端口 8501 是否被佔用
4. 檔案是否完整複製

---

**最後更新**: 2025年12月26日  
**應用版本**: 1.0 (包含雙語標題、PDF 下載、自動翻譯)
